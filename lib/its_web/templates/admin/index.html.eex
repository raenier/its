<br>
<div class="container-fluid">
    <ul class="nav nav-tabs">
      <li class="nav-item">
        <%= link to: admin_path(@conn, :index_all), class: "nav-link " <> if(@active_tab == 1, do: "active", else: "") do %>
          <span class="fas fa-user-tie"></span>&nbsp; All Users
        <% end %>
      </li>
      <li class="nav-item">
        <%= link to: admin_path(@conn, :index_clients), class: "nav-link " <> if(@active_tab == 2, do: "active", else: "") do %>
          <span class="fas fa-user-astronaut"></span>&nbsp; Call Centers
        <% end %>
      </li>
      <li class="nav-item">
        <%= link to: admin_path(@conn, :index_tech), class: "nav-link " <> if(@active_tab == 3, do: "active", else: "") do %>
          <span class="fas fa-user-secret"></span>&nbsp; Technician
        <% end %>
      </li>
      <div class="ml-auto">
        <a data-toggle="modal" data-target="#addUserModal" class="btn btn-success btn-sm">
          <span class="fas fa-user-plus gi-15x"></span>
        </a>
      </div>
    </ul>
    <br>
    <div class="card">
    <div class="card-body">
    <table class="table table-sm table-hover no-top-border">
      <thead>
        <tr>
          <th>User Name</th>
          <th>Name</th>
          <th>Type</th>
          <th>Password</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <%= for user <- @users do %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.last_name %> <%= user.first_name %>, <%= unless is_nil(user.middle_name), do: String.first(user.middle_name) %></td>
          <td><%= user.type %></td>
          <td>
            <div class="input-group col-7">
              <input type=password class="form-control form-control-sm" disabled value=<%= user.password %> id="pass<%= user.id %>">
              </input>
              <div class="input-group-append">
                <button class="btn btn-sm" onclick="reveal('pass<%= user.id %>')">
                  <span class="far fa-eye"></span>
                </button>
              </div>
            </div>
          </td>

          <td class="text-right">
            <button class="btn btn-outline-info btn-sm" data-toggle="modal" data-target="#editUserModal<%=user.id%>">
                <span class="fas fa-marker"></span>
            </button>
              <%= link to: admin_path(@conn, :delete, user, @active_tab), method: "delete", class: "btn btn-outline-danger btn-sm",
                data: [confirm: "Delete User?"] do %>
                <span class="fas fa-trash"></span>
              <% end %>
          </td>
        </tr>
        <!--EDIT MODAL-->
          <div id="editUserModal<%=user.id%>" class="modal fade" role="dialog">
            <div class="modal-dialog">
                <div class="modal-content">
                      <div class="modal-header">
                        <h5 class="modal-title">
                          <i class="fas fa-user-edit"></i>&nbsp; Edit User
                        </h5>
                          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                          </button>
                      </div>
                    <div class="modal-body">
                      <%= render "form_edit.html", changeset: Its.Accounts.change_user(user),
                                              action: admin_path(@conn, :update, user) %>
                    </div>
                    <div class="modal-footer" style="margin-left: 2px;">
                    </div>
                </div>
            </div>
          </div>
      <% end %>
      </tbody>
    </table>
  </div>
  </div>
</div>


<!--ADDMODAL-->
<div id="addUserModal" class="modal fade" role="dialog">
  <div class="modal-dialog">
      <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title">
              <i class="fas fa-user-tie"></i>&nbsp; Add User
            </h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
          </div>
          <div class="modal-body">
              <%= render "form.html", Map.put(assigns, :action, admin_path(@conn, :create)) %>
          </div>
          <div class="modal-footer" style="margin-left: 2px;">
          </div>
      </div>
  </div>
</div>
